# Example environment variables for backend (do NOT commit real secrets)
DB_USER=radius
DB_PASS=radius
DB_HOST=localhost
DB_NAME=radius
SECRET_KEY=change_this_secret
JWT_SECRET_KEY=change_this_jwt_secret
FLASK_ENV=development
